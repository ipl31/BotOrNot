<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:BotOrNot.Avalonia.ViewModels"
        xmlns:models="using:BotOrNot.Core.Models"
        xmlns:conv="using:BotOrNot.Avalonia.Converters"
        x:Class="BotOrNot.Avalonia.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Title="Bot or Not?"
        Width="1100"
        Height="800">

    <Window.Resources>
        <conv:BotColorConverter x:Key="BotColorConverter" />
        <conv:BotBorderColorConverter x:Key="BotBorderColorConverter" />
    </Window.Resources>

    <Design.DataContext>
        <vm:MainWindowViewModel/>
    </Design.DataContext>

    <Grid RowDefinitions="Auto,3*,Auto,2*,Auto,Auto">

        <!-- Top Bar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="8" Spacing="8">
            <Button x:Name="OpenButton" Content="Open Replay..." Click="OpenReplay_Click" />
            <Button x:Name="ColumnsButton" Content="Columns" />
            <ProgressBar IsIndeterminate="True"
                         IsVisible="{Binding IsLoading}"
                         Width="100"
                         Height="20"
                         Margin="8,0,0,0"/>
        </StackPanel>

        <!-- Error Banner -->
        <Border Grid.Row="0"
                Background="#FFDDDD"
                BorderBrush="#FF6666"
                BorderThickness="1"
                Padding="8"
                Margin="8,40,8,0"
                CornerRadius="4"
                IsVisible="{Binding ErrorMessage, Converter={x:Static StringConverters.IsNotNullOrEmpty}}">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#CC0000" />
        </Border>

        <!-- All Players Grid -->
        <DataGrid Grid.Row="1"
                  x:Name="PlayersGrid"
                  ItemsSource="{Binding Players}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserReorderColumns="True"
                  CanUserResizeColumns="True"
                  CanUserSortColumns="True"
                  GridLinesVisibility="Horizontal"
                  Margin="8,0,8,8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="280" IsVisible="False" />
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="180" />
                <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="80" />
                <DataGridTemplateColumn Header="Bot" Width="50">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate x:DataType="models:PlayerRow">
                            <Ellipse Width="14" Height="14"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center"
                                     Fill="{Binding Bot, Converter={StaticResource BotColorConverter}}"
                                     Stroke="{Binding Bot, Converter={StaticResource BotBorderColorConverter}}"
                                     StrokeThickness="1.5" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Platform" Binding="{Binding Platform}" Width="100" />
                <DataGridTextColumn Header="Kills" Binding="{Binding Kills}" Width="60" />
                <DataGridTextColumn Header="DeathCause" Binding="{Binding DeathCause}" Width="140" />
                <DataGridTextColumn Header="Pickaxe" Binding="{Binding Pickaxe}" Width="150" IsVisible="False" />
                <DataGridTextColumn Header="Glider" Binding="{Binding Glider}" Width="150" IsVisible="False" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Owner Eliminations Header -->
        <TextBlock Grid.Row="2"
                   Text="{Binding OwnerKillsHeader}"
                   FontWeight="Bold"
                   FontSize="14"
                   Margin="8,8,8,4" />

        <!-- Owner Eliminations Grid -->
        <DataGrid Grid.Row="3"
                  x:Name="OwnerEliminationsGrid"
                  ItemsSource="{Binding OwnerEliminations}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserReorderColumns="True"
                  CanUserResizeColumns="True"
                  CanUserSortColumns="True"
                  GridLinesVisibility="Horizontal"
                  Margin="8,0,8,8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="280" IsVisible="False" />
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="180" />
                <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="80" />
                <DataGridTemplateColumn Header="Bot" Width="50">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate x:DataType="models:PlayerRow">
                            <Ellipse Width="14" Height="14"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center"
                                     Fill="{Binding Bot, Converter={StaticResource BotColorConverter}}"
                                     Stroke="{Binding Bot, Converter={StaticResource BotBorderColorConverter}}"
                                     StrokeThickness="1.5" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Platform" Binding="{Binding Platform}" Width="100" />
                <DataGridTextColumn Header="Kills" Binding="{Binding Kills}" Width="60" />
                <DataGridTextColumn Header="DeathCause" Binding="{Binding DeathCause}" Width="140" />
                <DataGridTextColumn Header="Pickaxe" Binding="{Binding Pickaxe}" Width="150" IsVisible="False" />
                <DataGridTextColumn Header="Glider" Binding="{Binding Glider}" Width="150" IsVisible="False" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Eliminations List -->
        <ListBox Grid.Row="4"
                 ItemsSource="{Binding Eliminations}"
                 Height="100"
                 Margin="8,0,8,8" />

        <!-- Metadata Box -->
        <TextBox Grid.Row="5"
                 Text="{Binding MetadataText}"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 Height="80"
                 Margin="8,0,8,8"
                 ScrollViewer.VerticalScrollBarVisibility="Auto" />
    </Grid>
</Window>